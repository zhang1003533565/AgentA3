数据结构：栈与队列

栈是只允许在栈顶进行插入和删除操作的线性表，遵循后进先出原则。入栈是在栈顶插入元素，出栈是删除并返回栈顶元素。若在空栈上执行出栈操作，会发生下溢。

队列是只允许在队尾插入、队头删除的线性表，遵循先进先出原则。入队是在队尾插入元素，出队是在队头删除元素。

循环队列使用取模运算移动 front 和 rear 指针，可以复用数组空间。常见实现中，front == rear 表示队空，(rear + 1) % maxSize == front 表示队满。

栈适用于括号匹配、表达式求值、递归调用和回溯。队列适用于任务排队、打印缓冲、广度优先搜索和消息调度。
