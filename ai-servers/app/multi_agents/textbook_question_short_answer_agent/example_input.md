数据结构：栈与队列

栈是一种受限线性表，只允许在栈顶进行插入和删除，特点是后进先出。栈的基本操作包括初始化、判空、入栈、出栈和读取栈顶元素。

队列是一种受限线性表，只允许在队尾插入、队头删除，特点是先进先出。队列的基本操作包括初始化、判空、入队、出队和读取队头元素。

顺序栈使用数组存储，优点是访问位置明确，缺点是容量固定。链栈使用链表存储，优点是容量灵活，缺点是需要额外指针空间。

普通顺序队列在多次入队和出队后可能出现假溢出。循环队列通过让数组下标循环变化解决该问题。

栈的应用包括括号匹配、表达式求值、递归和回溯。队列的应用包括任务调度、打印队列、消息缓冲和广度优先搜索。
