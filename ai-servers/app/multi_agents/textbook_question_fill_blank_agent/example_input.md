数据结构：栈与队列

栈的操作端称为栈顶，非操作端称为栈底。栈遵循后进先出原则，英文常写作 LIFO。入栈是插入元素，出栈是删除栈顶元素。

队列的插入端称为队尾，删除端称为队头。队列遵循先进先出原则，英文常写作 FIFO。

顺序栈通常用数组和栈顶指针实现。链栈通常用链表实现。

循环队列通过 front 和 rear 指针配合取模运算实现循环使用。常见判空条件为 front == rear，常见判满条件为 (rear + 1) % maxSize == front。

栈常用于括号匹配、表达式求值和函数调用；队列常用于任务调度、缓冲区和广度优先搜索。
