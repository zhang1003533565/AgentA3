数据结构：栈

栈是一种只允许在栈顶进行插入和删除的线性表，遵循后进先出原则。

入栈：将元素插入栈顶。若使用数组实现顺序栈，需要先判断栈是否已满。

出栈：删除并返回栈顶元素。若栈为空，则不能执行出栈。

取栈顶元素：返回栈顶元素但不删除该元素。

括号匹配算法：从左到右扫描字符串。遇到左括号时入栈；遇到右括号时，如果栈为空则匹配失败，否则弹出栈顶左括号并判断类型是否对应。扫描结束后，如果栈为空则括号匹配成功，否则匹配失败。

括号类型包括圆括号 ()、方括号 [] 和花括号 {}。
