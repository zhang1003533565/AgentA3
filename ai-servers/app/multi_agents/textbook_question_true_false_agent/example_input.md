数据结构：栈与队列

栈是后进先出的线性表，只能在栈顶进行插入和删除操作。读取栈顶元素不会改变栈内元素数量。对空栈执行出栈操作是不合法的。

队列是先进先出的线性表，元素从队尾进入，从队头离开。读取队头元素不会改变队列长度。

顺序栈使用连续存储空间，容量固定时需要判断栈满。链栈使用链式存储，通常不需要预先设置最大容量。

普通顺序队列可能出现假溢出，循环队列可以复用数组前部释放出来的空间。

循环队列采用牺牲一个存储单元的方式时，front == rear 表示队空，(rear + 1) % maxSize == front 表示队满。
