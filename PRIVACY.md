# 隐私与数据使用说明

## 数据类别

系统可能处理以下数据，实际范围取决于启用功能：

- 账号资料：用户名、角色、姓名、手机号、邮箱、学院、专业、班级、学号/个人编号。
- 学习数据：画像维度与证据、知识掌握度、学习路径、资源浏览/完成/忽略记录、试卷答案、成绩和错题。
- 对话与生成数据：问题、提示词、模型回答、引用、生成文档/图片/视频及下载记录。
- 校园数据：课表、活动、论坛内容、食堂/设施偏好、地图坐标与导航记录。
- 技术数据：会话 ID、请求时间、错误类型、缓存命中和性能指标。
- 管理配置：模型、MaxKB、地图、对象存储连接信息；密钥属于凭据，不是普通业务数据。

## 使用目的

数据只应用于用户请求的校园服务、个性化学习画像、资源生成、路径推荐、学习效果评估、故障排查和安全审计。不得把学生数据用于未披露的模型训练、广告画像或向无关第三方出售。

## 数据流与第三方

- AppWeb/AppFrontend 将鉴权请求发送到 Java 后端。
- Java 保存业务事实到 MySQL，并把必要的 AI 请求代理给 Python。
- Python 可将短期会话/工作流状态写入 Redis，并通过 Java 访问校园业务与 MaxKB。
- 管理员配置的模型、MaxKB、地图和对象存储可能接收完成请求所必需的数据；部署者必须核对相应区域、保留和训练条款。
- 不应把整份学生档案发送给模型；只发送完成当前任务所需的最小字段。

## 保留与删除

- Python 对话记忆当前 TTL 为 2 小时；Redis 不可用时会退化为进程内存，进程退出即丢失。
- MySQL 中的画像、考试、路径、会话和资源记录目前没有统一自动保留策略，会持续存在到业务删除或数据库清理；这是上线前必须补齐的策略缺口。
- 上传文件和生成附件的保留时间依各模块配置；未形成统一用户可见策略前，不得承诺具体天数。
- 评测报告只应使用专用演示账号，并在提交前移除个人信息、令牌、内部 ID 和不可再分发内容。

## 用户权利与管理员职责

真实部署应提供访问、更正、导出、删除和撤回可选处理的渠道，并记录处理结果。当前仓库未实现统一的隐私请求门户；比赛演示由团队管理员人工处理，商业化前必须产品化并符合适用法律。

管理员应：

1. 使用最小权限和专用演示账号。
2. 在采集前说明用途与第三方处理者。
3. 禁止在截图、视频、日志和知识库源文件中出现真实学生隐私。
4. 对数据备份、导出、删除和权限变更留痕。
5. 发生泄露时立即停止相关服务、轮换凭据、保存审计证据并按适用规则通知。

## 比赛提交边界

提交包只应包含团队有权提交的代码、匿名测试数据、知识来源清单和真实但去标识化的验证结果。`artifacts/knowledge-base/python-course/manifest.json` 为 `needs_export` 时，不能从线上知识库抓取内容填充提交包。
