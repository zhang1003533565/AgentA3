# A3 最终提交核对清单

## 当前判定

代码离线质量门禁已通过，但作品**尚不能标记为最终可提交**：知识库、在线评测/压测、容器实启、许可证 P0 和 PPT/视频仍缺真实证据。以下勾选只代表本仓库当前证据，不代表人工口头确认。

## 已完成并有证据

- [x] Python 学习主线保留原校园功能入口，相关 Node 回归通过。
- [x] Java、Python、AppWeb、AppFrontend 离线测试/静态门禁通过。
- [x] 五服务 Compose 清单包含 MySQL、Redis、Java、AI Server、AppWeb，静态配置通过。
- [x] Java/Python 使用同一 `AI_INTERNAL_TOKEN`，Java/Redis 地址支持容器环境变量。
- [x] 30 题金标结构冻结（20+5+5），评分规则与阈值已写入仓库。
- [x] 压测计划固定为至少 5 并发、50 请求，不允许降量伪造。
- [x] NOTICE、第三方直接依赖、安全、隐私、AI Coding 披露已创建。
- [x] 开发说明中的外部 MaxKB/模型依赖没有被伪装成随镜像可用。

## P0：提交前必须完成

### 代码与环境冻结

- [ ] 将所有已审核任务提交整合到最终分支，解决冲突后重新运行 `bash scripts/ci/quality-gate.sh`。
- [ ] Git 工作区干净；记录最终 Git SHA、分支、构建时间和工具版本。
- [ ] 从干净机器/干净缓存构建五个服务所需镜像，记录镜像 digest，不使用 `latest`。
- [ ] 使用真实 `deploy/.env`（不入 Git）执行 `up -d`、`ps`、`bash deploy/verify.sh`。
- [ ] 导入非破坏性演示数据；只使用匿名测试账号，不运行会截断业务表的历史 `data.sql`。

### Python 课程知识库

- [ ] 冻结真实 MaxKB 版本、文档数、分段数、切分参数、课程章节与来源 ID。
- [ ] 对每份可提交源文件确认作者、来源、再分发许可和 SHA-256。
- [ ] 按 `artifacts/knowledge-base/python-course/README.md` 将 manifest 从 `needs_export` 改为 `ready`。
- [ ] 提供合法的 `portable-pack.zip` 或确定性导入说明，并在空环境完成恢复测试。
- [ ] 将 25 道可回答题的 `expectedEvidence` 映射到已声明来源，运行默认知识校验器。

### 真实 AI 与闭环证据

- [ ] 管理端完成 MaxKB 连接测试、Python 知识库绑定和模型配置测试。
- [ ] 真实生成至少 5 类资源；建议展示讲解、思维导图、混合题库、拓展阅读、代码实操、PPT/多模态中的 6 类。
- [ ] 每类资源保存请求、SSE 进度、最终文件、证据 ID、审核状态和失败重试结果。
- [ ] 使用同一匿名学生展示画像补问→资源包→学习路径→答题→掌握度/路径版本变化。
- [ ] 展示一条资料不足拒答和一条保留的校园 Leader 路径。

### 评测与性能

- [ ] 设置真实 `EVAL_ENDPOINT/EVAL_TOKEN`，运行 30 题，保留原始 `python-course-factual.json`。
- [ ] 两名成员按 rubric 复核 5 道解释题并签字；指标未过阈值不得改金标或阈值。
- [ ] 设置真实 `LOAD_ENDPOINT/LOAD_TOKEN`，运行 5×50 压测，保留失败请求与原始报告。
- [ ] 确认事实/压测 artifact 状态为 `completed`，所有 PPT 数字与 artifact 一致。

### 安全、隐私与许可证

- [ ] 指定真实私密安全联系人和漏洞响应时限。
- [ ] 运行 secret/PII 扫描；轮换发现的任何真实凭据，检查 Git 历史、日志、截图、PPT、视频和 artifact。
- [ ] 补齐 `wordpapergenerate` 模板来源/作者/版本/授权，或替换为团队有权提交的模板。
- [ ] 为 PyMuPDF 确定 AGPL 或商业许可路径并履行相应义务。
- [ ] 确认 Tencent COS SDK、AppFrontend `compressorjs` 实际许可证并生成锁文件。
- [ ] 生成最终 Java/Python/npm/容器传递依赖 SBOM 与完整许可证文本包。
- [ ] 演示环境仅使用匿名账号；商业化前完成密码自适应哈希迁移与统一数据保留/删除机制。

### 比赛提交物

- [ ] 源码、数据集/知识恢复包、模型/部署配置整理规范，清除缓存、构建产物、`.env` 和个人绝对路径。
- [ ] 系统开发说明书包含需求、架构、智能体职责、数据流、提示/工具边界、测试和部署。
- [ ] 测试说明书引用 `docs/verification/test-matrix.md` 和原始 artifact。
- [ ] 12 页左右演示 PPT 与 PDF 完成；所有数字在备注中链接到真实证据路径。
- [ ] 演示视频控制在 7 分钟内，建议 6:40–6:55；字幕、脚本、镜头表齐全。
- [ ] 视频展示操作、至少 5 类资源、多智能体进度、路径闭环、拒答和校园能力，不只播放静态页面。
- [ ] AI Coding 说明放在提交文档显著位置。
- [ ] 最终提交 manifest 列出每个文件的 SHA-256、最终 Git SHA、知识包哈希和模型标识。
- [ ] 在另一台机器按文档从零复现一次，由非开发成员签字确认。

## 打包排除项

提交前确认压缩包不含：

- `.env`、真实 API Key/JWT/内部令牌、云密钥、数据库备份中的真实个人数据；
- `.venv`、`node_modules`、`target`、`dist`、IDE 配置、系统缓存和本机绝对路径符号链接；
- 未授权课程原文、未知来源模板、外部账号/知识库 ID；
- 状态仍为 `not_run` 却在 PPT 中宣称已通过的评测数字；
- 状态仍为 `needs_export` 却命名为“完整知识库恢复包”的空目录。

## 最终四人复核（必须真实填写）

| 角色 | 姓名 | 日期 | 最终 SHA/签字 |
| --- | --- | --- | --- |
| 技术负责人 |  |  |  |
| 测试负责人 |  |  |  |
| 数据/知识库负责人 |  |  |  |
| 文档与演示负责人 |  |  |  |

空白表格不是完成证据。
